<!-- BEGIN ai-os (managed by ~/.ai-os/bootstrap/init-project.sh — edit the hub, not here) -->
## AI Operating System

Before any work in this repo, read and follow `~/.ai-os/AGENT-CONTRACT.md`
(the always-on contract: non-negotiables + read-order map). It links the rest of
the hub: `workflows.md`, `coding-standards.md`, `agent-coordination.md`,
`lessons.md`, and the `debug/` team.

**Coordination:** this repo uses `.ai/` for cross-agent scratch
(`JOURNAL.md` / `LOCKS.md` / `HANDOFF.md`, gitignored). Run the §Start check in
`~/.ai-os/agent-coordination.md` before editing. Durable/unfinished work → issue tracker.

Per-project rules in this file (below this block) override the generic hub rules
on project specifics. When they conflict and it's unclear which wins, ask.
<!-- END ai-os -->
